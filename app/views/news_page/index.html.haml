= scrivito_tag_list :ul, @obj, :toclist do |list, child|
  = list.tag :li do
    = link_to child.display_title, scrivito_path(child)
= scrivito_tag :h1, @obj, :title
%p "Created at: #{@obj.created_at}"
= scrivito_image_tag @obj, :image
= scrivito_tag :div, @obj, :body
