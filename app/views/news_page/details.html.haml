= scrivito_medium_dialog do
  = scrivito_details_for "Title" do
    = scrivito_tag :div, @obj, :title
  = scrivito_details_for "Tags" do
    = scrivito_tag :div, @obj, :tags
  = scrivito_details_for "Valid from" do
    = scrivito_tag :div, @obj, :valid_from
  = scrivito_details_for "Valid until" do
    = scrivito_tag :div, @obj, :valid_until